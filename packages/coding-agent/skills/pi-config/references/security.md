# Security 配置要点（信任/沙箱边界）

## 信任机制

- 信任决策存 `~/.pi/agent/trust.json`，按规范化目录路径保存；当前目录或父目录最近一条决策优先于全局默认
- **项目信任判定**：cwd 下存在 `.pi/` 或当前目录/祖先目录存在 `.agents/skills` 即视为有信任输入
- 交互启动时按 `defaultProjectTrust`（默认 `"ask"`）弹信任询问；信任后允许加载 `.pi/settings.json`、`.pi` 下扩展/技能/提示/主题/system prompt、缺失的项目包、项目级扩展
- 非交互模式（`-p`、`--mode json/rpc`）无提示：`"ask"`/`"never"` 忽略门禁资源，`"always"` 信任；`--approve`/`-a`、`--no-approve`/`-na` 单次覆盖
- 信任解析前只加载：上下文文件（AGENTS.md/CLAUDE.md）、用户/全局扩展、CLI `-e` 扩展；扩展可处理 `project_trust` 事件

## 重要：Pi 无内置沙箱

- 内置工具与扩展（TypeScript 模块）以进程权限读写文件、执行 shell
- **项目信任只是"输入加载守卫"，不限制模型后续调工具，也不隔离不信任的代码/提示注入**
- 仓库文件、注释、文档、构建输出中的提示注入属预期的本地 agent 风险

## 不可信/无人监控任务的隔离做法

- 在容器/VM/微 VM/远程或策略沙箱中运行整个 pi 进程；或宿主跑 pi、把内置工具执行路由进 Gondolin 微 VM
- 只挂载工作区所需路径；**不要挂载宿主 `~/.pi/agent`**（含会话/设置/凭据）；只传最小必要 API key 或短时凭据；限制网络；写回前审查 diff/输出
- 需要防意外写入时用只读挂载或拷入拷出（bind-mount 可读写仍能改宿主文件）

## 常见坑

- 项目信任不是安全边界，不做沙箱；隔离责任在用户侧
- 安全问题走仓库 SECURITY 政策，不要开公开 issue
## 文档兜底（本文件不足时）

本文件为要点提炼，遇到以下情况**必须**转查阅官方文档，禁止凭猜测继续：

- 字段含义、格式、允许值不确定
- 需要默认值、生效范围、生效方式等细节
- 本文件未覆盖的场景

```text
read(path: "~/.local/share/pi/docs/security.md")
```

对应官方文档：`security.md`。查阅方法见 `pi-docs-reference`。

文档仍无法覆盖时：查看现有配置文件作为参考，并如实告知用户文档未覆盖该主题。
